# 六上數學自學網站：單元製作 SOP

> 本文件說明製作一個單元頁面的標準流程，依據第 1~3 單元實作經驗整理。  
> 適用對象：協助製作或維護本網站的任何人。

---

## 一、製作前準備

### 1.1 確認教材資源
開啟本地資料夾中的版權 PDF（不上傳 GitHub）：
- `01_數學6上課本PDF/`：確認單元小節、例題、概念順序
- `02_數學6上習作PDF/`：取得練習題與應用題靈感
- `03_數學6上教冊PDF/`：了解教學目標、常見迷思、建議活動
- `04_數學6上習教PDF/`：取得習作解答與詳解

### 1.2 列出單元架構
將課本內容轉換為網頁區塊：

```
暖身區（1 個）
概念教學區（依課本小節數量，通常 2~4 個）
重點整理（1 個）
綜合練習（1 個，5~10 題）
資優加油站（1 個，2~4 題挑戰題）
```

### 1.3 規劃互動點
為每個小節至少設計 1 個互動視覺化或練習：
- 格子圖、線段圖、面積模型
- 滑桿、按鈕、輸入框
- 動畫演示（如燈泡同步、裝瓶過程）
- 即時檢查的選擇題或填充題

### 1.4 搜尋資優題
上網搜尋該單元的經典挑戰題，參考來源：均一教育平台、實力數學教室、南一書局資優試題本、數學競賽題庫。僅供靈感與改編，不可直接複製版權內容。

---

## 二、建立檔案

### 2.1 複製模板
若該單元已有 placeholder，直接開啟 `units/unitXX.html` 修改。若無檔案，複製任一已完成單元，刪除內容後保留骨架。

### 2.2 設定單元色彩
依單元主題調整漸層與主色，參考 `index.html` 的色碼規範。

### 2.3 修改基本資訊
- `<title>` 標題
- Hero 區的單元名稱與 tag
- Footer 的單元編號

---

## 三、製作各區塊內容

### 3.1 暖身區
1. 撰寫 1~2 句情境故事。
2. 設計一個可操作的互動。
3. 用視覺化即時顯示結果。

### 3.2 概念教學區
每個小節一個 `<section class="section" id="sec-xxx">`：
1. 用 `.section-header` 標示小節編號與標題。
2. 用 `.concept-box` 寫關鍵概念。
3. 用 `.interactive-area` 放互動元件。
4. 結尾放 1~2 題即時檢查練習。

### 3.3 重點整理
用 `.mini-cards` 或條列整理本單元口訣與關鍵概念。

### 3.4 綜合練習
- 5~10 題混合題型，涵蓋各小節。
- 可選擇題或填充題。
- 全部答對顯示完成區塊與彩帶。

### 3.5 資優加油站
- 2~4 題挑戰題。
- 每題附提示或解析摺疊區。
- 全部答對顯示挑戰完成區塊與彩帶。

---

## 四、JavaScript 開發規範

### 4.1 必備工具函數
每個單元應包含：
- `$(id)`：取得 DOM 元素
- `showFeedback(id, correct, msg)`：顯示回饋
- `addStar(n)`：累加星星
- `fireConfetti()`：彩帶特效
- 進度追蹤：`IntersectionObserver` 監聽各 section

### 4.2 分數相關單元補充
若單元涉及分數，需額外加入：
- `parseFrac(s)`：解析分數、帶分數
- `simplify(f)`：約分
- `fracEqual(a, b)`：判斷相等
- `checkFracInput(inputId, expected, fbId)`：檢查答案

### 4.3 命名規則
- HTML id：使用小寫駝峰或 kebab-case，保持同一單元內一致。
- 函數名：`checkXxx()` 檢查答案、`updateXxx()` 更新視覺、`showXxx()` 顯示結果。
- section id：`sec-warmup`、`sec-xxx`、`sec-review`、`sec-challenge`。

---

## 五、題目與回饋設計

### 5.1 答案檢查
- 數字答案：用 `parseInt()` 或 `parseFloat()` 比對。
- 分數答案：用 `checkFracInput()`，允許帶分數與最簡分數。
- 文字答案：統一去除空白並轉小寫後比對。

### 5.2 回饋文字
- 答對：給予具體肯定，例如「答對了！因為……」。
- 答錯：給予提示，不要只說「錯了」。
- 未輸入：提醒「請輸入答案」。

---

## 六、視覺化素材

### 6.1 CSS 可繪製的圖形
優先使用 CSS / SVG 繪製：
- 格子圖（grid）
- 線段圖（flex + 百分比寬度）
- 圓形、扇形（SVG circle）
- 簡單圖示（emoji 或 CSS shape）

### 6.2 需要製作 PNG 的情境
當圖形較複雜或需要精細插圖時，用圖片：
- 生活化物品（如中國結、披薩）
- 複雜幾何圖
- 課本截圖改編（注意版權）

圖片存放於 `units/images/`，命名建議：`unitXX_描述.png`。

---

## 七、測試流程

### 7.1 語法檢查
```bash
node --check unitXX.html 中的 JavaScript
```
實際做法：將 `<script>` 內容抽出存為 `.js` 後執行 `node --check`。

### 7.2 功能測試
- [ ] 每個按鈕、輸入框、下拉選單都能正常運作
- [ ] 答對時加星星，答錯時給予回饋
- [ ] 進度條會隨滾動更新
- [ ] 完成 100% 時出現完成區塊與彩帶
- [ ] 資優題全對時出現挑戰完成區塊與彩帶

### 7.3 畫面測試
- [ ] 手機版（寬度 375px）畫面正常
- [ ] 平板版（寬度 768px）畫面正常
- [ ] 無元素溢出或重疊
- [ ] 圖片有 alt 文字

---

## 八、上線前檢查

### 8.1 內容正確性
- [ ] 所有數學計算與答案經過驗算
- [ ] 分數答案已化為最簡分數
- [ ] 小節順序與課本一致
- [ ] 沒有錯別字或語句不通順

### 8.2 技術檢查
- [ ] JavaScript 語法檢查通過
- [ ] 無 console 錯誤
- [ ] 所有連結正確（回上一頁、單元連結）
- [ ] 圖片路徑正確且檔案存在

### 8.3 版權檢查
- [ ] 未上傳任何 PDF 至 GitHub
- [ ] 未直接複製版權習作題目文字
- [ ] 使用的圖片為自行繪製或合法授權

---

## 九、Commit 與 Push

### 9.1 Commit Message 格式
```
第 X 單元：簡短說明重點內容
修正第 X 單元：簡短說明修正項目
第 X 單元新增資優加油站：題目主題
```

### 9.2 推送後驗證
1. 推送至 `master`。
2. 等待 1~2 分鐘讓 GitHub Pages 更新。
3. 開啟 `https://clique520.github.io/math-grade6-semester1/units/unitXX.html` 確認。

---

## 十、製作後更新文件

### 10.1 更新工作表
開啟 `工作表.md`，將該單元與資優加油站的狀態改為 🟢，並填寫日期與備註。

### 10.2 檢視計畫書
若製作過程發現計畫書有需調整之處（如新發現的互動方式、色碼調整），同步更新 `網站建置計畫書.md`。

---

## 十一、常見問題與解法

### Q1：互動元件在手機上跑版？
使用 `@media (max-width: 500px)` 調整尺寸，或將 `flex` 改為 `column` 排列。

### Q2：分數輸入學生寫法不一致？
使用 `parseFrac()` 支援整數、分數、帶分數，並用 `simplify()` 後比對。

### Q3：進度條沒有到 100%？
檢查 `sections` 陣列是否包含所有 section id，且每個 id 都與 HTML 對應。

### Q4：彩帶沒有出現？
確認 `initConfetti()` 或 canvas 設定在 `window.onload` 中執行，且 `fireConfetti()` 被正確呼叫。

---

**最後更新**：2026-08-08
